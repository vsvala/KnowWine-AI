{
  "name": "back",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd ../front && npm install && npm run build && rm -rf ../back/dist && cp -r dist ../back/dist",
    "start": "npm run build && node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Virva Svala",
  "license": "MIT",
  "description": "KnowWine AI -backend",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
